services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.pi
    container_name: llamativo-admin-backend
    restart: unless-stopped
    ports:
      - "3000:8080"
    environment:
      - PORT=8080
    env_file:
      - .env
    shm_size: '2gb'
